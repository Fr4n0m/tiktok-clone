import styles from "./styles.module.css";
import PlaceholderPage from "../../components/PlaceholderPage";

const Inbox = () => {
  return <PlaceholderPage className={styles.inbox} />;
};

export default Inbox;
