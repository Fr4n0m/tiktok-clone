import styles from "./styles.module.css";
import PlaceholderPage from "../../components/PlaceholderPage";

const Live = () => {
  return <PlaceholderPage className={styles.live} />;
};

export default Live;
