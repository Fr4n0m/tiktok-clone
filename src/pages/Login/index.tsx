import styles from "./styles.module.css";
import PlaceholderPage from "../../components/PlaceholderPage";

const Login = () => {
  return <PlaceholderPage className={styles.login} />;
};

export default Login;
