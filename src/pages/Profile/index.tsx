import styles from "./styles.module.css";
import PlaceholderPage from "../../components/PlaceholderPage";

const Profile = () => {
  return <PlaceholderPage className={styles.profile} />;
};

export default Profile;
